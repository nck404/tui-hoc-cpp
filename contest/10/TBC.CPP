#include <iostream>
#include <fstream>
#include <algorithm>
#include <bits/stdc++.h>
#include <math.h>
using namespace std;
const long long Max_N = 1e9;

int  main() {
    ifstream cin("TBC.INP");
    ofstream cout("TBC.OUT");
    long long n;
    cin >> n ; long long a[n];
    for (int i = 0; i < n; ++i) cin >> a[i];
    for (int i = 0; i < n; ++i)
    
    return   0;
}
